@model CreateSlideVM

<section class="content-main">
    <div class="content-header">
        <div>
            <h2 class="content-title card-title">Create Slide</h2>
            <p>Add a new slide</p>
        </div>
    </div>
    <div class="card">
        <div class="card-body">
            <div class="row">
                <div class="col-md-12">
                    <form method="post" enctype="multipart/form-data">

                        <!-- Image Upload -->
                        <div class="mb-4">
                            <label asp-for="Photo" class="form-label">Image</label>
                            <input asp-for="Photo" class="form-control" accept="image/*" />
                            <span asp-validation-for="Photo" class="text-danger"></span>
                        </div>

                        <!-- Name Input -->
                        <div class="mb-4">
                            <label asp-for="Name" class="form-label">Name</label>
                            <input asp-for="Name" type="text" placeholder="Type here" class="form-control" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <!-- Order Input -->
                        <div class="mb-4">
                            <label asp-for="Order" class="form-label">Order</label>
                            <input asp-for="Order" type="text" placeholder="Type here" class="form-control" />
                            <span asp-validation-for="Order" class="text-danger"></span>
                        </div>

                        <!-- Title Input -->
                        <div class="mb-4">
                            <label asp-for="Title" class="form-label">Title</label>
                            <textarea asp-for="Title" placeholder="Type here" class="form-control" rows="3"></textarea>
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <!-- SubTitle Input -->
                        <div class="mb-4">
                            <label asp-for="SubTitle" class="form-label">Subtitle</label>
                            <textarea asp-for="SubTitle" placeholder="Type here" class="form-control" rows="3"></textarea>
                            <span asp-validation-for="SubTitle" class="text-danger"></span>
                        </div>

                        <!-- Submit Button -->
                        <button type="submit" class="btn btn-primary" style="width: auto; padding-left: 20px; padding-right: 20px;">Create Slide</button>
                        <button asp-action="index" class="btn btn-secondary" style="width: auto; padding-left: 20px; padding-right: 20px;">Cancel</button>

                    </form>
                </div>
            </div>
        </div>
    </div>
</section>